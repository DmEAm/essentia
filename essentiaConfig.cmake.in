
@PACKAGE_INIT@

if (CMAKE_VERSION VERSION_LESS 2.8.3)
  get_filename_component (CMAKE_CURRENT_LIST_DIR ${CMAKE_CURRENT_LIST_FILE} PATH)
endif ()

set (ESSENTIA_CONFIG_FILE "${CMAKE_CURRENT_LIST_FILE}")

set (ESSENTIA_LIBRARIES essentia)
set (ESSENTIA_LIBRARY_DIR "@CMAKE_INSTALL_FULL_LIBDIR@")
set (ESSENTIA_INCLUDE_DIR "@CMAKE_INSTALL_FULL_INCLUDEDIR@")

include ("${CMAKE_CURRENT_LIST_DIR}/essentiaLibraryDepends.cmake")

if (CMAKE_VERSION VERSION_LESS 2.8.3)
  set (CMAKE_CURRENT_LIST_DIR)
endif ()
